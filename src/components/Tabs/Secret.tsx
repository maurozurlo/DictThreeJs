import type { TabProps } from '../../types/Tabs'

// Secret tab content renders in the ActionPanel (SecretPanel.tsx), not the main area.
const Secret = (_props: TabProps) => null

export default Secret
